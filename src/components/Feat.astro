---

export interface FeatItems {
  Icon:string;
  Name: string;
}

interface Props {
  icons: FeatItems[];
}

const { icons } = Astro.props;
---
<ol class="bg-white"> 
  { 
    icons.map((icons) => (
    <h3 class="text-lg font-semibold text-[#125767]">
    {icons.Icon}-{icons.Name}))
  }   
</ol>